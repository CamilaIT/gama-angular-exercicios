import { CampoYearPipe } from './campo-year.pipe';

describe('CampoYearPipe', () => {
  it('create an instance', () => {
    const pipe = new CampoYearPipe();
    expect(pipe).toBeTruthy();
  });
});
